{
  "name": "@sigac/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sigac/agenda-preparation": "workspace:*",
    "@sigac/archive-operations": "workspace:*",
    "@sigac/audit": "workspace:*",
    "@sigac/tenant": "workspace:*",
    "@sigac/vale-archivo": "workspace:*",
    "drizzle-orm": "0.45.2",
    "pg": "latest"
  },
  "devDependencies": {
    "@types/pg": "latest",
    "typescript": "latest",
    "vitest": "4.1.10"
  }
}
