{
  "name": "@sigac/agenda-preparation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sigac/audit": "workspace:*",
    "@sigac/domain-kernel": "workspace:*",
    "@sigac/tenant": "workspace:*",
    "htmlparser2": "9.1.0",
    "domutils": "3.1.0",
    "iconv-lite": "0.6.3"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "4.1.10"
  }
}