openapi: 3.1.0
info:
  title: SIGAC API
  version: 0.3.23
servers:
  - url: /api/v1
paths:
  /health:
    get:
      security: []
      responses:
        '200':
          description: API health
  /session:
    get:
      operationId: getSessionAuthorization
      responses:
        '200':
          description: Authorization projection for the authenticated actor
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SessionAuthorizationReadModel' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
  /ubicaciones:
    get:
      operationId: listUbicaciones
      responses:
        '200':
          description: Operational locations in the active tenant; an empty catalog returns an empty items array.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UbicacionesResponse' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
  /expedientes:
    get:
      operationId: searchExpedientesByNumero
      parameters:
        - in: query
          name: numero
          required: true
          schema: { type: string, minLength: 1 }
      responses:
        '200':
          description: Expediente matches in the active tenant; zero matches returns an empty items array.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExpedienteSearchResponse' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
  /expedientes/{id}:
    get:
      operationId: getExpediente
      parameters:
        - $ref: '#/components/parameters/ExpedienteId'
      responses:
        '200':
          description: Operational Expediente Workspace read model
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExpedienteReadModel' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/ExpedienteNotFound' }
  /expedientes/{id}/timeline:
    get:
      operationId: getExpedienteTimeline
      parameters:
        - $ref: '#/components/parameters/ExpedienteId'
        - in: query
          name: cursor
          required: false
          description: Opaque cursor returned by the preceding page.
          schema: { type: string }
        - in: query
          name: limit
          required: true
          schema: { type: integer, minimum: 1 }
      responses:
        '200':
          description: Cursor-paginated Expediente movement timeline
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TimelinePage' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/ExpedienteNotFound' }
  /expedientes/{id}/audit:
    get:
      operationId: getExpedienteAudit
      parameters:
        - $ref: '#/components/parameters/ExpedienteId'
        - in: query
          name: cursor
          required: false
          description: Opaque cursor returned by the preceding audit page.
          schema: { type: string }
        - in: query
          name: limit
          required: true
          schema: { type: integer, minimum: 1 }
      responses:
        '200':
          description: Sanitized audit entries ordered by occurredAt and auditId descending
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExpedienteAuditPage' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404': { $ref: '#/components/responses/ExpedienteNotFound' }
  /expedientes/{id}/dispatch:
    post:
      operationId: dispatchExpediente
      parameters:
        - $ref: '#/components/parameters/ExpedienteId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DispatchExpedienteRequest' }
      responses:
        '204':
          description: Expediente dispatched successfully; no response body.
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/ExpedienteNotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /expedientes/{id}/accept-custody:
    post:
      operationId: acceptCustody
      parameters:
        - $ref: '#/components/parameters/ExpedienteId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AcceptCustodyRequest' }
      responses:
        '204':
          description: Custody accepted successfully; no response body.
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/ExpedienteNotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /agenda-imports:
    post:
      operationId: importAgenda
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Agenda import processed successfully
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaImportResponse' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
        '409': { $ref: '#/components/responses/IdempotencyKeyReused' }
        '413': { $ref: '#/components/responses/AgendaUploadTooLarge' }
        '415': { $ref: '#/components/responses/AgendaArtifactUnsupported' }
        '422': { $ref: '#/components/responses/AgendaLayoutRejected' }
        '500': { $ref: '#/components/responses/AgendaImportFailed' }
        '504': { $ref: '#/components/responses/AgendaImportTimeout' }
    get:
      operationId: listAgendaImports
      parameters:
        - in: query
          name: agendaDate
          required: false
          description: Filter by agenda date (YYYY-MM-DD).
          schema: { type: string, format: date }
        - in: query
          name: cursor
          required: false
          description: Opaque cursor returned by the preceding page.
          schema: { type: string }
        - in: query
          name: limit
          required: true
          schema: { type: integer, minimum: 1 }
      responses:
        '200':
          description: Cursor-paginated agenda import history
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaImportHistoryPage' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
  /agenda-imports/{importacionId}:
    get:
      operationId: getAgendaImportDetail
      parameters:
        - in: path
          name: importacionId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Full detail of an agenda importación
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaImportDetail' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
        '404': { $ref: '#/components/responses/AgendaImportNotFound' }
  /agenda-imports/{importacionId}/incidents:
    get:
      operationId: getAgendaImportIncidents
      parameters:
        - in: path
          name: importacionId
          required: true
          schema: { type: string, format: uuid }
        - in: query
          name: cursor
          required: false
          description: Opaque cursor returned by the preceding page.
          schema: { type: string }
        - in: query
          name: limit
          required: true
          schema: { type: integer, minimum: 1 }
      responses:
        '200':
          description: Cursor-paginated incidents for an agenda importación
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaImportIncidentsPage' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
  /agendas/{date}:
    get:
      operationId: getAgendaDaySummary
      parameters:
        - in: path
          name: date
          required: true
          schema: { type: string, format: date }
      responses:
        '200':
          description: Day-level agenda summary read model
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaDayReadModel' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
        '404': { $ref: '#/components/responses/AgendaNotFound' }
  /agendas/{date}/preparation-items:
    get:
      operationId: getAgendaPreparationList
      parameters:
        - in: path
          name: date
          required: true
          schema: { type: string, format: date }
        - in: query
          name: order
          required: false
          schema:
            type: string
            enum: [APPOINTMENT_TIME_ASC, PATIENT_NAME_ASC]
        - in: query
          name: cursor
          required: false
          description: Opaque cursor returned by the preceding page.
          schema: { type: string }
        - in: query
          name: limit
          required: true
          schema: { type: integer, minimum: 1 }
      responses:
        '200':
          description: Cursor-paginated agenda preparation items
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaPreparationPage' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
  /agendas/{date}/preparation-items/print:
    get:
      operationId: printAgendaPreparationList
      parameters:
        - in: path
          name: date
          required: true
          schema: { type: string, format: date }
        - in: query
          name: order
          required: false
          schema:
            type: string
            enum: [APPOINTMENT_TIME_ASC, PATIENT_NAME_ASC]
      responses:
        '200':
          description: Full (non-paginated) preparation list for print
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgendaPreparationPrintResponse' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
  /agendas/{date}/preparation-report:
    post:
      operationId: generatePreparationReport
      summary: Genera y descarga el PDF de lista de preparación para Archivo Clínico
      description: |
        Genera un PDF operativo agrupado por servicio y médico, listo para imprimir.
        El PDF se genera en memoria y se descarga directamente (sin persistencia).
        Requiere AGENDA_VIEW + AGENDA_PRINT.
        La fecha y el tenant se obtienen del contexto de autorización; nunca del body.
        Ref: preparation-reports spec REQ-PR-002, ADR-0030, ADR-0031.
      parameters:
        - in: path
          name: date
          required: true
          schema: { type: string, format: date, example: "2026-08-26" }
        - in: query
          name: order
          required: false
          description: Ordenamiento de citas. Default APPOINTMENT_TIME_ASC.
          schema:
            type: string
            enum: [APPOINTMENT_TIME_ASC, PATIENT_NAME_ASC]
        - in: query
          name: services
          required: false
          description: |
            Lista de códigos de servicio separados por coma, ej. "CL05,CL06".
            Omitir o dejar vacío para incluir todos los servicios.
          schema: { type: string }
      responses:
        '200':
          description: PDF generado correctamente, descarga directa
          headers:
            Content-Disposition:
              description: attachment; filename="lista-preparacion-{date}.pdf"
              schema: { type: string }
          content:
            application/pdf:
              schema: { type: string, format: binary }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/AgendaPermissionDenied' }
        '422': { $ref: '#/components/responses/NoActiveAppointments' }
        '500': { $ref: '#/components/responses/AgendaImportFailed' }

  # ── Vale Archivo (SM 1-14) — T-34 ──────────────────────────────────────────
  /vale-archivo:
    post:
      operationId: crearValeArchivo
      summary: Registra un nuevo ValeArchivo (formato SM 1-14)
      tags: [ValeArchivo]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateValeArchivoRequest' }
      responses:
        '201':
          description: Vale creado
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ValeArchivoCreatedResponse' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '409':
          description: Conflict — ya existe un ValeArchivo con ese número en este tenant
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 409 }
                      code: { const: VALE_NUMERO_DUPLICADO }
        '422':
          description: Unprocessable — vale requiere al menos un ítem o transición inválida
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 422 }
                      code: { const: VALE_REQUIERE_ITEMS }
    get:
      operationId: listarValesArchivo
      summary: Lista ValeArchivos con paginación cursor-based y filtros opcionales
      tags: [ValeArchivo]
      parameters:
        - name: estado
          in: query
          schema:
            type: string
            enum: [RECIBIDA, EN_BUSQUEDA, COMPLETA, PARCIAL, NO_LOCALIZADA, ENTREGADA, CERRADA]
        - name: fecha
          in: query
          schema: { type: string, format: date }
        - name: unidad
          in: query
          schema: { type: string }
        - name: cursor
          in: query
          schema: { type: string }
          description: Opaque cursor returned by the preceding page.
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Lista paginada de vales
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ValeArchivoPage' }
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
  /vale-archivo/{id}:
    get:
      operationId: consultarValeArchivo
      summary: Devuelve el detalle completo de un ValeArchivo
      tags: [ValeArchivo]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Detalle del vale
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ValeArchivoDetail' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404':
          description: Vale no encontrado
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 404 }
                      code: { const: VALE_ARCHIVO_NOT_FOUND }
  /vale-archivo/{id}/iniciar-busqueda:
    post:
      operationId: iniciarBusquedaValeArchivo
      summary: Transiciona el vale de RECIBIDA a EN_BUSQUEDA
      tags: [ValeArchivo]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Búsqueda iniciada correctamente.
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404':
          description: Vale no encontrado
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 404 }
                      code: { const: VALE_ARCHIVO_NOT_FOUND }
        '422':
          description: Transición de estado inválida
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 422 }
                      code: { const: INVALID_STATE_TRANSITION }
  /vale-archivo/{id}/items/{itemId}:
    patch:
      operationId: registrarLocalizacionValeArchivo
      summary: Registra el resultado de búsqueda de un ítem (LOCALIZADO / NO_LOCALIZADO)
      tags: [ValeArchivo]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: itemId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegistrarLocalizacionRequest' }
      responses:
        '200':
          description: Localización registrada correctamente.
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404':
          description: Vale o ítem no encontrado
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 404 }
        '422':
          description: Transición de estado inválida
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 422 }
                      code: { const: INVALID_STATE_TRANSITION }
  /vale-archivo/{id}/entrega:
    post:
      operationId: registrarEntregaValeArchivo
      summary: Registra la entrega de expedientes al receptor final (COMPLETA|PARCIAL → ENTREGADA)
      tags: [ValeArchivo]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegistrarEntregaRequest' }
      responses:
        '200':
          description: Entrega registrada correctamente.
        '400': { $ref: '#/components/responses/HttpValidationError' }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404':
          description: Vale no encontrado
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 404 }
                      code: { const: VALE_ARCHIVO_NOT_FOUND }
        '422':
          description: Transición de estado inválida
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 422 }
                      code: { const: INVALID_STATE_TRANSITION }
  /vale-archivo/{id}/cerrar:
    post:
      operationId: cerrarValeArchivoAdministrativo
      summary: Cierre administrativo de un vale en estado NO_LOCALIZADA → CERRADA
      tags: [ValeArchivo]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                motivo: { type: string, description: 'Motivo de cierre administrativo (opcional).' }
      responses:
        '200':
          description: Vale cerrado administrativamente.
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404':
          description: Vale no encontrado
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 404 }
                      code: { const: VALE_ARCHIVO_NOT_FOUND }
        '422':
          description: Transición de estado inválida
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 422 }
                      code: { const: INVALID_STATE_TRANSITION }
  /vale-archivo/{id}/pdf:
    get:
      operationId: generarPdfValeArchivo
      summary: Genera y descarga el PDF SM 1-14 del vale
      tags: [ValeArchivo]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: PDF SM 1-14 generado en memoria
          headers:
            Content-Disposition:
              schema:
                type: string
                example: 'attachment; filename="sm1-14-VA-2026-00142-2026-08-26.pdf"'
          content:
            application/pdf:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/AuthenticationRequired' }
        '403': { $ref: '#/components/responses/PermissionDenied' }
        '404':
          description: Vale no encontrado
          content:
            application/json:
              schema:
                allOf:
                  - { $ref: '#/components/schemas/ProblemDetails' }
                  - type: object
                    properties:
                      status: { const: 404 }
                      code: { const: VALE_ARCHIVO_NOT_FOUND }
components:
  parameters:
    ExpedienteId:
      in: path
      name: id
      required: true
      schema: { type: string, format: uuid }
  schemas:

    # ── Vale Archivo schemas ─────────────────────────────────────────────────
    EstadoVale:
      type: string
      enum: [RECIBIDA, EN_BUSQUEDA, COMPLETA, PARCIAL, NO_LOCALIZADA, ENTREGADA, CERRADA]

    EstadoBusqueda:
      type: string
      enum: [PENDIENTE, LOCALIZADO, NO_LOCALIZADO]

    SolicitanteReferencia:
      type: object
      required: [nombre, cargo]
      properties:
        nombre: { type: string }
        cargo:  { type: string }

    ValeArchivoItemInput:
      type: object
      required: [expedienteNumero, pacienteNombre, especialidad]
      properties:
        expedienteNumero: { type: string }
        pacienteNombre:   { type: string }
        especialidad:     { type: string }

    CreateValeArchivoRequest:
      type: object
      required:
        - numeroVale
        - fechaSolicitud
        - fechaRecepcion
        - unidadSolicitante
        - solicitanteNombre
        - solicitanteCargo
        - autorizadorNombre
        - autorizadorCargo
        - items
      properties:
        numeroVale:        { type: string }
        fechaSolicitud:    { type: string, format: date }
        fechaRecepcion:    { type: string, format: date }
        unidadSolicitante: { type: string }
        solicitanteNombre: { type: string }
        solicitanteCargo:  { type: string }
        autorizadorNombre: { type: string }
        autorizadorCargo:  { type: string }
        items:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/ValeArchivoItemInput' }

    ValeArchivoCreatedResponse:
      type: object
      required: [id, numeroVale, estado]
      properties:
        id:         { type: string, format: uuid }
        numeroVale: { type: string }
        estado:     { $ref: '#/components/schemas/EstadoVale' }

    ValeArchivoSummary:
      type: object
      required: [id, numeroVale, fechaSolicitud, unidadSolicitante, solicitanteNombre, estado, itemCount]
      properties:
        id:                { type: string, format: uuid }
        numeroVale:        { type: string }
        fechaSolicitud:    { type: string, format: date }
        unidadSolicitante: { type: string }
        solicitanteNombre: { type: string }
        estado:            { $ref: '#/components/schemas/EstadoVale' }
        itemCount:         { type: integer, minimum: 1 }

    ValeArchivoPage:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/ValeArchivoSummary' }
        nextCursor:
          type: string
          nullable: true
          description: Opaque cursor returned by the preceding page.

    ValeArchivoItemDetail:
      type: object
      required: [id, expedienteNumero, pacienteNombre, especialidad, estadoBusqueda]
      properties:
        id:                  { type: string, format: uuid }
        expedienteNumero:    { type: string }
        pacienteNombre:      { type: string }
        especialidad:        { type: string }
        estadoBusqueda:      { $ref: '#/components/schemas/EstadoBusqueda' }
        ubicacionEncontrada: { type: string, nullable: true }
        observaciones:       { type: string, nullable: true }

    ValeArchivoDetail:
      type: object
      required:
        - id
        - numeroVale
        - fechaSolicitud
        - fechaRecepcion
        - unidadSolicitante
        - solicitante
        - autorizador
        - estado
        - creadoPor
        - items
        - createdAt
        - actualizadoEn
      properties:
        id:                  { type: string, format: uuid }
        numeroVale:          { type: string }
        fechaSolicitud:      { type: string, format: date }
        fechaRecepcion:      { type: string, format: date }
        unidadSolicitante:   { type: string }
        solicitante:         { $ref: '#/components/schemas/SolicitanteReferencia' }
        autorizador:         { $ref: '#/components/schemas/SolicitanteReferencia' }
        estado:              { $ref: '#/components/schemas/EstadoVale' }
        creadoPor:           { type: string }
        busquedaIniciadaPor: { type: string, nullable: true }
        busquedaIniciadaAt:  { type: string, format: date-time, nullable: true }
        entregadoPor:        { type: string, nullable: true }
        entregadoAt:         { type: string, format: date-time, nullable: true }
        receptorEntrega:     { type: string, nullable: true }
        createdAt:           { type: string, format: date-time }
        actualizadoEn:        { type: string, format: date-time }
        items:
          type: array
          items: { $ref: '#/components/schemas/ValeArchivoItemDetail' }

    RegistrarLocalizacionRequest:
      type: object
      required: [estadoBusqueda]
      properties:
        estadoBusqueda:      { type: string, enum: [LOCALIZADO, NO_LOCALIZADO] }
        ubicacionEncontrada: { type: string }
        observaciones:       { type: string, maxLength: 500 }

    RegistrarEntregaRequest:
      type: object
      required: [receptorEntrega, entregadoAt, itemsEntregados]
      properties:
        receptorEntrega:  { type: string }
        entregadoAt:      { type: string, format: date-time }
        itemsEntregados:
          type: array
          items: { type: string, format: uuid }

    Permission:
      type: string
      enum: [REQUEST_CREATE, REQUEST_ASSIGN, SEARCH_START, SEARCH_MARK_LOCATED, SEARCH_MARK_NOT_LOCATED, PREPARATION_MARK_READY, CUSTODY_TRANSFER, EXPEDIENT_DISPATCH, CUSTODY_ACCEPT, LOAN_OPEN, LOAN_RENEW, RETURN_RECEIVE, REARCHIVE_CONFIRM, INCIDENT_OPEN, INCIDENT_RESOLVE, EXPEDIENT_VIEW, EXPEDIENT_AUDIT_VIEW, LOCATION_VIEW, REPORT_VIEW, ADMIN_CONFIGURE, AGENDA_IMPORT, AGENDA_VIEW, AGENDA_INCIDENT_VIEW, AGENDA_PRINT, ARCHIVE_REQUEST_VIEW, ARCHIVE_REQUEST_PROCESS, ARCHIVE_REQUEST_DELIVER]
    SessionAuthorizationReadModel:
      type: object
      additionalProperties: false
      required: [actorId, permissions]
      properties:
        actorId: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/Permission' }
    DecimalBigint:
      type: string
      pattern: '^[0-9]+$'
      example: '42'
    EstadoOperativo:
      type: string
      enum: [DISPONIBLE, APARTADO, EN_TRASLADO, EN_CONSULTA, NO_LOCALIZADO, EXTRAVIADO]
    Ubicacion:
      type: object
      additionalProperties: false
      required: [id, codigo, descripcion]
      properties:
        id: { type: string, format: uuid }
        codigo: { type: string }
        descripcion: { type: string }
    UbicacionOption:
      type: object
      additionalProperties: false
      required: [id, codigo, descripcion]
      properties:
        id: { type: string, format: uuid }
        codigo: { type: string }
        descripcion: { type: string }
    UbicacionesResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/UbicacionOption' }
    ExpedienteSearchPaciente:
      type: object
      additionalProperties: false
      required: [idInstitucional, curp, nombreOperativo, numeroIssste]
      properties:
        idInstitucional: { type: string }
        curp: { type: string }
        nombreOperativo: { type: string }
        numeroIssste: { type: string }
    ExpedienteSearchItem:
      type: object
      additionalProperties: false
      required: [expedienteId, expedienteNumero, paciente, estadoOperativo, ubicacion]
      properties:
        expedienteId: { type: string, format: uuid }
        expedienteNumero: { type: string }
        paciente: { $ref: '#/components/schemas/ExpedienteSearchPaciente' }
        estadoOperativo: { $ref: '#/components/schemas/EstadoOperativo' }
        ubicacion:
          oneOf:
            - { $ref: '#/components/schemas/Ubicacion' }
            - { type: 'null' }
    ExpedienteSearchResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/ExpedienteSearchItem' }
    PacienteReferenciaSummary:
      type: object
      additionalProperties: false
      required: [id, displayLabel]
      properties:
        id: { type: string }
        displayLabel: { type: string }
    CustodiaActual:
      type: object
      additionalProperties: false
      required: [custodioTipo, custodioRef, servicio, aceptadaEn]
      properties:
        custodioTipo: { type: string }
        custodioRef: { type: string }
        servicio: { type: [string, 'null'] }
        aceptadaEn: { type: [string, 'null'], format: date-time }
    ActiveRequestSummary:
      type: object
      additionalProperties: false
      required: [solicitudId, tipo, origen, estado, asignadoA]
      properties:
        solicitudId: { type: string }
        tipo: { type: string }
        origen: { type: string }
        estado:
          type: string
          enum: [Pendiente, Asignada, EnBusqueda, Localizada, Preparada, Entregada, Cancelada, NoLocalizada]
        asignadoA: { type: [string, 'null'] }
    ActiveLoanSummary:
      type: object
      additionalProperties: false
      required: [prestamoId, finalidad, custodioRef, destinoTipo, destinoRef, dueAt, fuenteHabilitanteSalida, estado]
      properties:
        prestamoId: { type: string }
        finalidad: { type: string }
        custodioRef: { type: string }
        destinoTipo: { type: string }
        destinoRef: { type: string }
        dueAt: { type: string, format: date-time }
        fuenteHabilitanteSalida:
          type: string
          enum: [CONSULTA_PROGRAMADA, VALE_ARCHIVO_SM_1_14, ORDEN_SUPERIOR]
        estado: { type: string, enum: [Activo, Vencido] }
    OpenIncidentSummary:
      type: object
      additionalProperties: false
      required: [incidenciaId, tipo, severidad, estado, resumen, asignadoA, openedAt]
      properties:
        incidenciaId: { type: string }
        tipo: { type: string }
        severidad: { type: string }
        estado: { type: string, enum: [Abierta, EnInvestigacion, Escalada] }
        resumen: { type: string }
        asignadoA: { type: [string, 'null'] }
        openedAt: { type: string, format: date-time }
    ExpedienteCapability:
      type: string
      enum:
        - SOLICITAR
        - INICIAR_BUSQUEDA
        - MARCAR_LOCALIZADO
        - MARCAR_NO_LOCALIZADO
        - DISPATCH
        - ACCEPT_CUSTODY
        - ABRIR_PRESTAMO
        - RENOVAR_PRESTAMO
        - RECIBIR_DEVOLUCION
        - CONFIRMAR_REARCHIVO
        - REPORTAR_INCIDENCIA
    ExpedienteReadModel:
      type: object
      additionalProperties: false
      required: [id, expedienteNumero, pacienteRef, estadoOperativo, ubicacionActual, custodiaActual, prestamoActivo, solicitudActiva, incidenciasAbiertas, capabilities, rowVersion]
      properties:
        id: { type: string, format: uuid }
        expedienteNumero: { type: string }
        pacienteRef: { $ref: '#/components/schemas/PacienteReferenciaSummary' }
        estadoOperativo: { $ref: '#/components/schemas/EstadoOperativo' }
        ubicacionActual:
          oneOf:
            - { $ref: '#/components/schemas/Ubicacion' }
            - { type: 'null' }
        custodiaActual:
          oneOf:
            - { $ref: '#/components/schemas/CustodiaActual' }
            - { type: 'null' }
        prestamoActivo:
          oneOf:
            - { $ref: '#/components/schemas/ActiveLoanSummary' }
            - { type: 'null' }
        solicitudActiva:
          oneOf:
            - { $ref: '#/components/schemas/ActiveRequestSummary' }
            - { type: 'null' }
        incidenciasAbiertas:
          type: array
          items: { $ref: '#/components/schemas/OpenIncidentSummary' }
        capabilities:
          type: array
          items: { $ref: '#/components/schemas/ExpedienteCapability' }
        rowVersion: { $ref: '#/components/schemas/DecimalBigint' }
    MovimientoExpedienteSummary:
      type: object
      additionalProperties: false
      required: [movimientoId, movementType, originLocation, destinationLocation, originCustodianRef, destinationCustodianRef, businessReferenceType, businessReferenceId, occurredAt, recordedAt, actorRef, source, correlationId]
      properties:
        movimientoId: { type: string }
        movementType: { type: string }
        originLocation: { type: [string, 'null'] }
        destinationLocation: { type: [string, 'null'] }
        originCustodianRef: { type: [string, 'null'] }
        destinationCustodianRef: { type: [string, 'null'] }
        businessReferenceType: { type: string }
        businessReferenceId: { type: [string, 'null'] }
        occurredAt: { type: string, format: date-time }
        recordedAt: { type: string, format: date-time }
        actorRef: { type: string }
        source: { type: string }
        correlationId: { type: [string, 'null'] }
    TimelinePage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/MovimientoExpedienteSummary' }
        nextCursor: { type: [string, 'null'] }
    ExpedienteAuditEntrySummary:
      type: object
      additionalProperties: false
      required: [auditId, action, result, actorRef, occurredAt, source, requestId, correlationId]
      properties:
        auditId: { type: string, format: uuid }
        action: { type: string }
        result: { type: string, enum: [success, denied, not-found, conflict, invalid-transition] }
        actorRef: { type: string }
        occurredAt: { type: string, format: date-time }
        source: { type: string, enum: [WEB, INTERNAL] }
        requestId: { type: string }
        correlationId: { type: string }
    ExpedienteAuditPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/ExpedienteAuditEntrySummary' }
        nextCursor: { type: [string, 'null'] }
    BusinessReference:
      type: object
      additionalProperties: false
      required: [type, id]
      properties:
        type: { type: string }
        id: { type: [string, 'null'] }
    IntendedCustodian:
      type: object
      additionalProperties: false
      required: [type, reference]
      properties:
        type: { type: string }
        reference: { type: string }
    CustodyReceptor:
      type: object
      additionalProperties: false
      required: [type, reference, service]
      properties:
        type: { type: string }
        reference: { type: string }
        service: { type: [string, 'null'] }
    DispatchExpedienteRequest:
      type: object
      additionalProperties: false
      required: [destination, intendedCustodian, businessReference, expectedRowVersion]
      properties:
        destination: { $ref: '#/components/schemas/Ubicacion' }
        intendedCustodian: { $ref: '#/components/schemas/IntendedCustodian' }
        businessReference: { $ref: '#/components/schemas/BusinessReference' }
        expectedRowVersion: { $ref: '#/components/schemas/DecimalBigint' }
    AcceptCustodyRequest:
      type: object
      additionalProperties: false
      required: [receptor, ubicacionDestino, businessReference, expectedRowVersion]
      properties:
        receptor: { $ref: '#/components/schemas/CustodyReceptor' }
        ubicacionDestino: { $ref: '#/components/schemas/Ubicacion' }
        businessReference: { $ref: '#/components/schemas/BusinessReference' }
        expectedRowVersion: { $ref: '#/components/schemas/DecimalBigint' }
    HttpFieldError:
      type: object
      additionalProperties: false
      required: [field, code]
      properties:
        field: { type: string }
        code: { type: string, enum: [REQUIRED, INVALID_FORMAT, INVALID_TYPE, OUT_OF_RANGE] }
    ProblemDetails:
      type: object
      additionalProperties: false
      required: [type, title, status, code]
      properties:
        type: { type: string, format: uri }
        title: { type: string }
        status: { type: integer }
        code:
          type: string
          enum: [HTTP_VALIDATION_ERROR, AUTHENTICATION_REQUIRED, PERMISSION_DENIED, INSUFFICIENT_ENABLING_SOURCE, EXPEDIENTE_NOT_FOUND, OPTIMISTIC_LOCK_CONFLICT, REQUEST_INVALID_TRANSITION, AGENDA_IMPORT_NOT_FOUND, AGENDA_NOT_FOUND, IDEMPOTENCY_KEY_REUSED, AGENDA_UPLOAD_TOO_LARGE, AGENDA_ARTIFACT_UNSUPPORTED, AGENDA_LAYOUT_REJECTED, AGENDA_IMPORT_FAILED, AGENDA_IMPORT_TIMEOUT]
        detail: { type: string }
        errors:
          type: array
          items: { $ref: '#/components/schemas/HttpFieldError' }
    AgendaImportMetrics:
      type: object
      additionalProperties: false
      required: [receivedRecords, processed, added, updated, unchanged, restored, pendingReview, rejected, duplicateFolio, withdrawnFromAgenda, incidents, errors]
      properties:
        receivedRecords: { type: integer, minimum: 0 }
        processed: { type: integer, minimum: 0 }
        added: { type: integer, minimum: 0 }
        updated: { type: integer, minimum: 0 }
        unchanged: { type: integer, minimum: 0 }
        restored: { type: integer, minimum: 0 }
        pendingReview: { type: integer, minimum: 0 }
        rejected: { type: integer, minimum: 0 }
        duplicateFolio: { type: integer, minimum: 0 }
        withdrawnFromAgenda: { type: integer, minimum: 0 }
        incidents: { type: integer, minimum: 0 }
        errors: { type: integer, minimum: 0 }
    AgendaImportSummary:
      type: object
      additionalProperties: false
      required: [importacionId, agendaDate, importedAt, outcome, metrics]
      properties:
        importacionId: { type: string, format: uuid }
        agendaDate: { type: string, format: date }
        importedAt: { type: string, format: date-time }
        outcome:
          type: string
          enum: [IMPORTED, ALREADY_IMPORTED, RECONCILED]
        metrics: { $ref: '#/components/schemas/AgendaImportMetrics' }
    AgendaImportResponse:
      type: object
      additionalProperties: false
      required: [importacionId, agendaDate, importedAt, outcome, metrics]
      properties:
        importacionId: { type: string, format: uuid }
        agendaDate: { type: string, format: date }
        importedAt: { type: string, format: date-time }
        outcome:
          type: string
          enum: [IMPORTED, ALREADY_IMPORTED, RECONCILED]
        metrics: { $ref: '#/components/schemas/AgendaImportMetrics' }
    AgendaImportHistoryPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AgendaImportSummary' }
        nextCursor: { type: [string, 'null'] }
    RegistroImportadoResult:
      type: object
      additionalProperties: false
      required: [registroId, sourcePosition, folio, processingResult, incidentCodes]
      properties:
        registroId: { type: string, format: uuid }
        sourcePosition: { type: integer, minimum: 1 }
        folio: { type: [string, 'null'] }
        processingResult:
          type: string
          enum: [ADDED, UPDATED, UNCHANGED, RESTORED, PENDING_REVIEW, REJECTED, DUPLICATE_FOLIO]
        incidentCodes:
          type: array
          items:
            type: string
            enum: [PHYSICIAN_NOT_RESOLVED, PHYSICIAN_AMBIGUOUS, SERVICE_NOT_RESOLVED, EXPEDIENT_NOT_RESOLVED, REQUIRED_DATA_MISSING, ROW_INCONSISTENT, DUPLICATE_FOLIO_IN_SNAPSHOT]
    AgendaImportDetail:
      type: object
      additionalProperties: false
      required: [summary, registros]
      properties:
        summary: { $ref: '#/components/schemas/AgendaImportSummary' }
        registros:
          type: array
          items: { $ref: '#/components/schemas/RegistroImportadoResult' }
    AgendaImportIncidentSummary:
      type: object
      additionalProperties: false
      required: [incidenciaId, registroId, sourcePosition, type]
      properties:
        incidenciaId: { type: string, format: uuid }
        registroId: { type: string, format: uuid }
        sourcePosition: { type: integer, minimum: 1 }
        type:
          type: string
          enum: [PHYSICIAN_NOT_RESOLVED, PHYSICIAN_AMBIGUOUS, SERVICE_NOT_RESOLVED, EXPEDIENT_NOT_RESOLVED, REQUIRED_DATA_MISSING, ROW_INCONSISTENT, DUPLICATE_FOLIO_IN_SNAPSHOT]
    AgendaImportIncidentsPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AgendaImportIncidentSummary' }
        nextCursor: { type: [string, 'null'] }
    AgendaDayReadModel:
      type: object
      additionalProperties: false
      required: [agendaDate, latestImportacionId, latestImportedAt, latestOutcome, activeAppointments, physicians, services, incidentCount]
      properties:
        agendaDate: { type: string, format: date }
        latestImportacionId: { type: string, format: uuid }
        latestImportedAt: { type: string, format: date-time }
        latestOutcome:
          type: string
          enum: [IMPORTED, ALREADY_IMPORTED, RECONCILED]
        activeAppointments: { type: integer, minimum: 0 }
        physicians: { type: integer, minimum: 0 }
        services: { type: integer, minimum: 0 }
        incidentCount: { type: integer, minimum: 0 }
    AgendaMedicoRef:
      type: object
      additionalProperties: false
      required: [numeroEmpleado, nombre]
      properties:
        numeroEmpleado: { type: string }
        nombre: { type: string }
    AgendaServicioRef:
      type: object
      additionalProperties: false
      required: [codigo, nombre]
      properties:
        codigo: { type: string }
        nombre: { type: string }
    AgendaExpedienteRef:
      type: object
      additionalProperties: false
      required: [original, reference]
      properties:
        original: { type: string }
        reference: { type: [string, 'null'] }
    AgendaPreparationItem:
      type: object
      additionalProperties: false
      required: [folio, nombrePaciente, expediente, tipoDerechohabiente, tipoConsulta, agendaDate, appointmentTime, medico, servicioEspecialidad]
      properties:
        folio: { type: string }
        nombrePaciente: { type: string }
        expediente: { $ref: '#/components/schemas/AgendaExpedienteRef' }
        tipoDerechohabiente: { type: string }
        tipoConsulta: { type: string, enum: [FIRST_TIME, SUBSEQUENT] }
        agendaDate: { type: string, format: date }
        appointmentTime: { type: string, pattern: '^[0-2][0-9]:[0-5][0-9]$' }
        medico: { $ref: '#/components/schemas/AgendaMedicoRef' }
        servicioEspecialidad: { $ref: '#/components/schemas/AgendaServicioRef' }
    AgendaPreparationPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AgendaPreparationItem' }
        nextCursor: { type: [string, 'null'] }
    AgendaPreparationPrintResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AgendaPreparationItem' }
  responses:
    HttpValidationError:
      description: Structurally invalid or malformed request
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                required: [detail]
                properties:
                  status: { const: 400 }
                  code: { const: HTTP_VALIDATION_ERROR }
    AuthenticationRequired:
      description: Authentication is required
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 401 }
                  code: { const: AUTHENTICATION_REQUIRED }
    PermissionDenied:
      description: Authenticated actor lacks EXPEDIENT_VIEW
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 403 }
                  code: { const: PERMISSION_DENIED }
    Forbidden:
      description: Authenticated actor lacks permission or an enabling source
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 403 }
                  code: { enum: [PERMISSION_DENIED, INSUFFICIENT_ENABLING_SOURCE] }
    ExpedienteNotFound:
      description: Expediente is not visible in the active tenant
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 404 }
                  code: { const: EXPEDIENTE_NOT_FOUND }
    Conflict:
      description: Optimistic lock conflict or invalid business transition
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 409 }
                  code: { enum: [OPTIMISTIC_LOCK_CONFLICT, REQUEST_INVALID_TRANSITION] }
    AgendaPermissionDenied:
      description: Authenticated actor lacks AGENDA_IMPORT or AGENDA_VIEW
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 403 }
                  code: { const: PERMISSION_DENIED }
    AgendaImportNotFound:
      description: Agenda importación not found in active tenant
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 404 }
                  code: { const: AGENDA_IMPORT_NOT_FOUND }
    AgendaNotFound:
      description: Agenda not found for this date in active tenant
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 404 }
                  code: { const: AGENDA_NOT_FOUND }
    IdempotencyKeyReused:
      description: Idempotency-Key already used with a different artifact
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 409 }
                  code: { const: IDEMPOTENCY_KEY_REUSED }
    AgendaUploadTooLarge:
      description: Uploaded file exceeds the configured limit
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 413 }
                  code: { const: AGENDA_UPLOAD_TOO_LARGE }
    AgendaArtifactUnsupported:
      description: Artifact format or extension is not supported
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 415 }
                  code: { const: AGENDA_ARTIFACT_UNSUPPORTED }
    AgendaLayoutRejected:
      description: File content does not match the recognized SIMEF HTML layout
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 422 }
                  code: { const: AGENDA_LAYOUT_REJECTED }
    AgendaImportFailed:
      description: Unexpected technical failure; rollback performed
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 500 }
                  code: { const: AGENDA_IMPORT_FAILED }
    AgendaImportTimeout:
      description: Import processing timed out; rollback performed
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 504 }
                  code: { const: AGENDA_IMPORT_TIMEOUT }
    NoActiveAppointments:
      description: No hay citas activas para los servicios solicitados en esta fecha
      content:
        application/problem+json:
          schema:
            allOf:
              - { $ref: '#/components/schemas/ProblemDetails' }
              - type: object
                properties:
                  status: { const: 422 }
                  code: { const: NO_ACTIVE_APPOINTMENTS }
